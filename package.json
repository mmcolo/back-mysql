{
  "name": "back-mysql",
  "version": "1.0.0",
  "description": "API express.js for a nodejs serve (we can use the phpmyadmin interfaces).",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "mmcolo",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "mysql": "^2.18.1"
  }
}
